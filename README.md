# Unemployment Analytics Dashboard

Interactive Streamlit deployment for the Unemployment Analysis with Python project.

## Local run

```bash
pip install -r requirements.txt
streamlit run app_unemployment.py
```

Keep the CSV beside `app_unemployment.py`. The app checks the expected unemployment filenames automatically.